- name: Install required packages
  ansible.builtin.apt:
    name:
      - snmpd
      - snmp
      - libsnmp-dev
    state: present
- name: Install pysnmp for testing SNMP on hosts
  ansible.builtin.pip:
    name: pysnmp
- name: Configure SNMP
  ansible.builtin.template:
    src: snmp.conf.j2
    dest: /etc/snmp/snmpd.conf
    mode: '777'
    owner: root
- name: Download the distro script for LibreNMS to identify OS version
  get_url:
    url: https://raw.githubusercontent.com/librenms/librenms-agent/master/snmp/distro
    dest: /usr/bin/distro
    mode: '0755'
- name: Start and enable SNMPd systemd service.
  ansible.builtin.systemd:
    name: snmpd
    state: started
    enabled: true
    daemon_reload: true
- name: Restart SNMPd systemd service if already running.
  ansible.builtin.systemd:
    name: snmpd
    state: restarted
